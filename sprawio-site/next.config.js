/** @type {import('next').NextConfig} */
const nextConfig = {
  // HTML-e projektu leżą w /public — Next.js serwuje je statycznie
  // Endpoint API: /api/generate-appeal → pages/api/generate-appeal.js
};

module.exports = nextConfig;
